server:
  port: 8082

spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: payment-service
  datasource:
    url: jdbc:h2:mem:payments
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  h2:
    console:
      enabled: true
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: payment-service
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: com.projetofinal.paymentservice.event
        spring.json.value.default.type: com.projetofinal.paymentservice.event.OrderCreatedEvent
        spring.json.use.type.headers: false

app:
  kafka:
    topic:
      pedidos: pedidos-criados

gateway:
  mercadopago:
    base-url: https://api.mercadopago.com
    access-token: ${MERCADOPAGO_ACCESS_TOKEN:TEST-ACCESS-TOKEN}
    public-key: ${MERCADOPAGO_PUBLIC_KEY:TEST-PUBLIC-KEY}
    payer-email: ${MERCADOPAGO_PAYER_EMAIL:comprador@teste.com}
