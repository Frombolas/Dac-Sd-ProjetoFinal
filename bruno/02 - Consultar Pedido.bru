meta {
  name: Consultar Pedido
  type: http
  seq: 2
}

get {
  url: {{orderBaseUrl}}/api/pedidos/{{pedidoId}}
  body: none
  auth: none
}

tests {
  test("deve retornar 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
