meta {
  name: Listar Pedidos
  type: http
  seq: 3
}

get {
  url: {{orderBaseUrl}}/api/pedidos
  body: none
  auth: none
}

tests {
  test("deve retornar 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
